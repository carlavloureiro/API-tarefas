const express = require("express");
const router = express.Router();
// const pool = require("../db"); // Se já estiver conectado ao DB

// ... Suas rotas CRUD vão aqui (router.get, router.post, etc.) ...

// EXPORTAÇÃO: Esta linha é obrigatória!
module.exports = router;